# Example .env for local development
# Replace values with your Neon (or other Postgres) credentials

DB_HOST=your-neon-host.neon.tech
DB_PORT=5432
DB_USER=your_db_user
DB_PASSWORD=your_password
DB_NAME=your_database

# App
PORT=3000
FRONTEND_URL=http://localhost:5173

# External API
TRANCO_API_BASE_URL=https://tranco.example/api/domain

# Cache TTL in hours
CACHE_TTL_HOURS=24

# Notes:
# - If you paste a full Neon connection string (postgres://...), parse it into the individual DB_* variables above.
# - Neon requires TLS/SSL; the app will enable ssl options automatically when DB_HOST contains 'neon'.
# - Do NOT commit real credentials to version control.
